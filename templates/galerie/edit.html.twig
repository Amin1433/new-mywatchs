{% extends 'base.html.twig' %}

{% block title %}Modifier Galerie{% endblock %}

{% block main %}
    <h1>Modifier Galerie</h1>

    {{ include('galerie/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('galerie/_delete_form.html.twig', {'button_label': 'Delete'}) }}

    <a href="{{ path('member_show', {'id': galerie.creator.id}) }}">back to account</a>

{% endblock %}
