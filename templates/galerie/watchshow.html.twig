{% extends 'base.html.twig' %}

{% block title %}watch n°{{ watch.id }}{% endblock %}

{% block main %}
    <h1>{{ watch.name }}</h1>

       {% dump watch %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ watch.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ watch.name }}</td>
            </tr>
            <tr>
    			<th>Image</th>
    			<td><img src="{{ vich_uploader_asset(watch, 'imageFile') }}"/></td>
			</tr>
            <tr>
                <th>Description</th>
                <td>{{ watch.description}}</td>
            </tr>
        </tbody>
    </table>
    &nbsp;
        {% if app.user and app.user.id == watch.rack.member.id %}
                    <a href="{{ path('app_watch_edit', {'id': watch.id}) }}">modifier</a>
        {% endif %}

	<br>
	<br>
	<a href="{{ path('app_galerie_show', { 'id': galerie.id}) }}">retour à la galerie</a>


{% endblock %} {# body #} 
{# ... #} 
